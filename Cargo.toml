[package]
name = "rtempest"
version = "0.1.0"
edition = "2021"

[features]
default = []

[dependencies]
log = "0.4.21"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0.114"
tokio = { version = "1.36", features = ["macros", "net", "rt-multi-thread", "sync"] }
